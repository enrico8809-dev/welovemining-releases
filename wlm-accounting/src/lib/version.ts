/**
 * Single source of truth for the version shown in the UI. Keep in step with
 * `expo.version` in app.json — it's what tells you which APK is on the phone.
 */
export const APP_VERSION = "1.6.1";
