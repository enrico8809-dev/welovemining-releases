# Local config holds your token + miner credentials — never commit it.
config.json
node_modules/
windows/node_modules/
*.log
daemon/

# Installer build artifacts (produced by CI)
installer/payload/
installer/Output/
